
/*o.run('#div-list', function(divId, that) {
    that.obj('tab', {
        liId: divId + ' .row-th li',
        type: 'right',
        isDrag: true,
        liCss: 'hover'
    });
});*/
d_import("head");